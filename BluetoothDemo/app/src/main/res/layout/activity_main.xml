<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <Button
            android:id="@+id/turnOffBluetoothButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="turnBluetoothOff"
            android:text="Turn Bluetooth off" />

        <Button
            android:id="@+id/findDevicesButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/turnOffBluetoothButton"
            android:onClick="findingDevices"
            android:text="Find devices" />

        <Button
            android:id="@+id/viewPairedDevicesButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/findDevicesButton"
            android:onClick="viewPairedDevices"
            android:text="View Paired Devices" />

        <ListView
            android:id="@+id/pairedDevicesListView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/viewPairedDevicesButton"
            android:layout_marginTop="0dp" />
    </RelativeLayout>
</androidx.constraintlayout.widget.ConstraintLayout>